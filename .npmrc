# Refuse to run under a Node or pnpm version the project does not claim to
# support, rather than failing later in a way that looks like our bug.
engine-strict=true

# The project has no dependencies and intends to keep it that way: adding one
# should be a deliberate act, visible in review.
save-exact=true
